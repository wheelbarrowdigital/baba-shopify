{% style %}
  .buttons {
    display: flex;
    flex: 1 0 fit-content;
    flex-flow: row wrap;
    gap: var(--spacing-4);
    justify-content: flex-start;

    @media (min-width: 576px) {
      flex-grow: 0;
    }
  }
{% endstyle %}
<div class="buttons">
  {{ content }}
</div>
